name: heroes
description: A new Flutter application.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.7.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^3.0.10
  flutter_modular: ^3.0.2
  mobx: ^1.2.1+4
  flutter_mobx: ^1.1.0+2
  anim_search_bar: ^2.0.1

  percent_indicator:
  cached_network_image:
  http:
  flutter_spinkit:
dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  mobx_codegen: ^1.1.2
  flutter_modular_test: ^1.0.0
flutter:
  uses-material-design: true